const car = {
    brand: "Peugeot",
    model: "2008",
    yearOfProduction: 2017,
    color: 'szary'
};

console.log(`Mój samochód to ${car.color} ${car.brand} ${car.model} z ${car.yearOfProduction} roku.`);